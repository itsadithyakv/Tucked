/**
 * fr-CA scaffold — same keys as en-CA, machine-checked by the type system.
 * Real translations land with the Quebec preset (Phase 3); until then these
 * placeholders keep the keys honest. Never ship this file's strings.
 */

import type { Strings } from './en-CA';
import { enCA } from './en-CA';

export const frCA: Strings = enCA; // TODO(fr-CA): translate with the Quebec preset
