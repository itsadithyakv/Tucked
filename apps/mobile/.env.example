# Copy to .env — values for the LOCAL Supabase stack (supabase start prints them).
# The anon key is public by design; real secrets never ship in EXPO_PUBLIC_* vars.
EXPO_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
EXPO_PUBLIC_SUPABASE_ANON_KEY=paste-local-anon-key-here
