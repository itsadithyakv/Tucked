# Compliance map

*Regulation section → where it lives in the product. Every row must eventually name its table(s), function(s), screen(s) and at least one test that fails if the rule is broken (tests are named for their section, e.g. `s72_attendance_requires_actual_times`). Sections are O. Reg. 137/15 unless noted. Source requirements: [tucked-ontario-requirements.md](../references/tucked-ontario-requirements.md); acceptance criteria: [build prompt §9](../references/tucked-build-prompt.md).*

Status: ⬜ planned · 🔶 in progress · ✅ built & tested. Phase = when it lands per the build plan.
Full audit with test evidence: [compliance-test-report.md](compliance-test-report.md) (522 pgTAP + 75 domain tests as of 2026-08-30).

| Section | Requirement (short) | Tables / functions | Screens | Tests | Phase | Status |
|---|---|---|---|---|---|---|
| Sched. 1 | Age-group presets: ratios, max group, qualified staff | `age_group`; `domain/ageGroups` | Room setup | `sched1_*` | 0 | ✅ |
| ss. 8–11 | Live ratios; reduced-ratio windows & floors; never count volunteers/students/RCs; 6+ children ⇒ 2 staff | `staff_shift`, `attendance_event`; `domain/ratios` | Room home, supervisor home | `s8_*`, `s11_*` | 0 (engine) / 1 (live) | ✅ engine + live room board |
| s. 11 (supervision) | Face-to-name headcounts at transitions, spot checks, drills, evacuations — never touching s. 72(3) attendance; drills auto-cross-ref into the DWR | `headcount_check`, `record_headcount`; `app.headcount_cross_reference` | Room board Headcount sheet, evacuation screen | `s11_*`, `part4_*`, `s37_drill_*` | 1 | ✅ PIN-signed counts with missing-child snapshots; model in [attendance-model.md](../references/attendance-model.md) |
| s. 32 | Daily health observation on arrival, incl. parent-reported symptoms | `care_log(health_observation)` | Sign-in flow | `s32_*` | 1 | ✅ observed-on-arrival prompt inside the sign-in flow, parent-reported notes, DB-validated |
| s. 33.1 | Back-to-sleep under 12 mo; direct visual sleep checks < 24 mo, per-child timestamped; rest ≤ 2 h | `care_log(sleep_check, nap_*)` | Room sleep board | `s33_1_*` | 1 | ✅ DB gating, room nap board with due prompts, console register |
| s. 35 | Immunisation per the local MOH, or the two standard exemption forms (medical: physician/NP-signed; conscience/religious: notarised), or attends school | `immunisation_record` append-only ledger + `current_immunisation` view; `record_immunisation` RPC | Children's records register + child detail (in the s. 72(6) MOH copy), console home exception | `s35_*` (14) | 1 | ✅ practitioner and notarisation DB-required; "attends school" age-gated at 44 months; history never rewritten |
| s. 36 | Sick child: separate, contact parent, record; exclusion & return per the centre's illness policy; public health notification and orders to the program advisor | `illness_policy` (auto-seeded, the source of every return criterion), `health_exclusion`, append-only `health_exclusion_contact`, `public_health_notification` (2 business days to the advisor, 1 for enforcement), `exclusion_status` view; **the sign-in block lives in `app.attendance_event_rules`** | Room device symptom picker + separation place; console Illness & exclusions; family card with the return criteria; home exceptions for unreached families and overdue advisor orders | `s36_*` (38) | 2 | ✅ an excluded child cannot be signed in; clearing is a signed judgment; early return needs a written reason |
| s. 36(4) | Accident report with evidence parent received a copy | `accident_report` + acknowledgement | Report flow, family ack | `s36_4_*` | 1 | ✅ full loop: on-device report form, auto Now alert (DB trigger), parent ack in-app = delivery evidence, console register |
| s. 37 | Daily written record: dated entry every operating day, auto-cross-referenced, human-closed | `daily_written_record`; 06:00 `pg_cron` draft; `app.dwr_compose` composes the day from its own rows and quotes each cross-reference verbatim; `regenerate_daily_record_draft`, `daily_record_preview` | Close-the-day flow with a live preview and Redraft | `s37_*` (28) | 1 | ✅ deterministic, never generated |
| s. 38 | Serious occurrence: 24 h CCLS clock, anonymised 10-business-day posting (never for abuse/neglect), updates, CAS duty; human files | `serious_occurrence` + updates/postings/children, `statutory_holiday`, `app.add_business_days`, hourly reminder cron | Console Serious occurrences page, home warnings, mobile Overview clock | `s38_*`, `cyfsa_*` | 1 | ✅ clock, filing evidence, fallback, anonymisation guard, business-day math, reminders — all pgTAP-proven |
| ss. 39, 39.1, 52 | Anaphylaxis / medical needs / special needs individualised plans, developed with the parent, agreed before implementation, reviewed annually; centre anaphylaxis policy; posted allergy list (s. 43(3)) | `individualised_plan` (versioned, immutable), `dietary_restriction`, `allergy_list` view, `centre.anaphylaxis_policy` | Console Plans & allergies (+ printable posting), family in-app agreement, room Allergies sheet, evacuation cache | `s39_*`, `s39_1_*`, `s52_*`, `s43_3_*` | 1 | ✅ required content DB-enforced, no active plan without agreement, allergy list live on every surface |
| s. 40 | Medication: authorisation with dose + schedule/symptoms; label checklist; designated staff; **blanket items logged** | `medication_authorisation`, `medication_administration` | Med flows | `s40_*` | 1 | ✅ DB rules, room administration flow (auth picker, dose, blanket items), registers + CSV |
| ss. 42–44 | Menus planned and posted for the current **and** following week, substitutions noted at the time, posted menus kept 30 days; infants fed per the parent's written instructions; special diets per written instructions | `menu_week` (draft→posted, frozen once posted), `menu_item`, `menu_substitution` (snapshots what was planned), `feeding_instruction` (from a consenting parent) | Console Menus (grid, post, printable posting, substitution log) + home exception; family More shows today's menu with substitutions; room device records the substitution at the time; feeding instructions in the room food sheet | `s42_*`, `s44_*` (22) | 1 | ✅ posted menus never deleted (clears the 30-day floor); drafts invisible to families |
| s. 45 | Parent handbook containing all 14 required items, given to every parent | `handbook_section_spec` (required list is jurisdiction data), `handbook_content` draft, `handbook_version`/`handbook_version_section` (immutable once issued), `handbook_acknowledgement`, `handbook_outstanding` view; `save_handbook_section`, `publish_handbook`, `acknowledge_handbook`, `record_handbook_acknowledgement` | Console Handbook (checklist, issue, printable, version history, who has it) + home exception; family app reads and acknowledges | `s45_*` (29) | 2 | ✅ incomplete handbook never issued; issued versions never edited; anaphylaxis policy and CWELCC status read from the live record |
| s. 47 | Outdoor play 2 h a day (weather permitting) for 6+ hour care; a child kept in needs a physician's or parent's written instruction | `outdoor_requirement` (jurisdiction data), `outdoor_period` (measured, never typed), `outdoor_shortfall` (reason → DWR), `outdoor_exemption`, `outdoor_day` view, `outdoor_by_child()` | Room device Outside/Come-in toggle with a live counter; console Outdoor play (by room, by child, exemptions) + home exception | `s47_*` (32) | 2 | ✅ age floor flagged for regulatory review |
| ss. 46, 48–52 | Program statement (HDLH); infants separated during active play | program statement is a handbook section (s. 45); `compliance_task` | Console | `s46_*`+ | 2 | 🔶 |
| s. 50 | Safe arrival & dismissal: expected-but-absent prompt; release only to authorised persons; restrictions hard-block | `pickup_authorisation`, `pickup_restriction` (SQL-level block) | Sign-in/out | `s50_*` | 1 | ✅ SQL hard block, identity-confirmed release flow, safe-arrival chase prompts with recorded outcomes |
| ss. 53–64 | Staff files: RECE, first aid/CPR, VSC (obtained before starting, ≤6 months old when obtained, 5-year renewal), offence declaration every year without a VSC, health assessment, immunisation | `staff_requirement` rule pack (jurisdiction × role), `credential` + s. 60 derivation trigger, `staff_document` + private `evidence` bucket scoped to subject and leadership, `staff_file_gaps()`, `offence_declaration_gaps()` | Console staff file: what is missing, record a credential, attach evidence, printable per-person file | `s53_*`, `s58_*`, `s60_*`, `s61_*` (34) | 1 | ✅ the file reports absence, not just presence |
| s. 72(1) | Children's record: all 11 items; missing = `not_applicable`/`parent_declined`, never blank | `child`, `child_record_item` | Enrolment | `s72_*` | 1 | 🔶 11 typed items, parent completion, verification ✅ |
| s. 72(3) | Attendance per age group: actual times or "absent"; offline; corrections carry who/when/why; continuous across room moves | `attendance_event` | Room sign-in/out, evacuation screen | `s72_attendance_*` | 1 | ✅ swipe sign-in/out, offline queue, evacuation, console register + CSV, supervisor corrections with who/when/why |
| s. 72(5) | Retention: 3 y post-discharge; financial 6 y (O. Reg. 138/15 s. 27.1); purge = anonymise-after, never before, never a delete | `retention_clock` (discharge trigger), `app.anonymise_child` + nightly `app.run_retention_sweep` (02:30); immutability triggers open only for the anonymiser, and close behind it | Console children page shows each discharged child's clock | `s72_5_*` (19) | 1 | ✅ identity scrubbed (name, DOB day/month, photos, record items, free text, headcount snapshots); regulated rows all survive; early anonymisation refused even when called directly |
| s. 72(6) | Medical officer of health export: items 2, 3, 8, 9 exactly | export fn | Console exports | `s72_6_*` | 1 | ✅ child-record page prints the exact s. 72(6) subset |
| s. 73 | Enrolment never blocked on an optional consent | `consent` model | Enrolment | `s73_*` | 1 | ✅ DB-enforced, pgTAP-proven |
| s. 75.1 | Waitlist: no fees; self-serve position without exposing others | `waitlist_entry` (no money-shaped column exists), append-only `waitlist_event`, `app.waitlist_rank`, `waitlist_position` view (care staff only), `waitlist_self_check` (anonymous, by code), `my_waitlist_positions` (signed-in parent), `app.run_waitlist_sweep` 02:45 cron | Console Waiting list + home exception for unanswered offers; public `/waiting-list` position check; family More card | `s75_1_*` (38) | 2 | ✅ policy text lives in the handbook (s. 45); the order is computed from it and never hand-set |
| s. 82(2) | Electronic records: staff & Ministry can always get in; offline on premises; no "ask your admin to unlock" | offline queue + zero-network evacuation cache; `break_glass_access` (PIN-signed, reason-required, read-only, 24 h self-expiry, loud + audited); `admin_grant_supervisor` continuity | Console home break-glass card + leadership alerts; /admin supervisor grant | `s82_2_*` (19) | 1 | ✅ read-only unlock of the inspection registers, no write ever granted, supervisors alerted on every open; platform admins can always restore supervisor access |
| Parts 4, 10 | Fire drills, alarm/equipment tests, playground inspections (daily/monthly/annual, CSA Z614), policy reviews — with written records; repair log with hazards restricted until fixed | `compliance_task` (auto-seeded per centre) + `compliance_completion` (append-only written records) + `compliance_issue`; drill headcounts auto-complete the drill task; hazards cross-ref into the DWR | Console compliance calendar + home exceptions; room-device Compliance checks card (the playground walk happens outside) | `part4_*`, `part10_*` (17) | 1 | ✅ note-required completions, cadence advances from completion date, leadership-only schedule changes |
| CWELCC / municipal | $22/day cap ENFORCED (not merely displayed); non-base fees restricted; 30-day disenrolment notice with delivery proof; CRA receipts; Toronto reporting | `cwelcc_parameter`, `app.cwelcc_eligible`, `fee_schedule`, `fee_item`, `fee_charge`/`fee_credit`/`fee_payment`, `household_balance`, `cra_receipt`(+lines), `cwelcc_withdrawal_notice`(+delivery), `app.run_financial_sweep` | Console Fees & receipts (schedule, ledger, printable receipt); family Fees card | `cwelcc_*`, `cra_*`, `oreg138_*` (43) | 2 | ✅ except the City's reporting formats, which need discovery |
| PIPEDA | Minimum collection; consent per purpose; access/deletion workflow, 30-day SLA; Canadian hosting | `consent`, RLS, `ca-central-1`; platform admins see tenancy & billing, never children | Console, /admin | `pipeda_*` | 0–1 | 🔶 RLS on every table; platform-admin child-blindness pgTAP-proven; access/deletion workflow P2 |
| O. Reg. 138/15 s. 27.1 | Financial records kept 6 years | `billing_event` append-only ledger; `centre_subscription` audited | /admin, console Plan & billing card | `oreg138_*` | 1 | ✅ ledger is insert-only, every event names who recorded it |
| §9.14 + platform | Billing state never hides or locks a regulated record | no policy anywhere reads `centre_subscription` | — | `never_cancelled_billing_*` | 1 | ✅ proven: cancelled subscription, supervisor still reads and writes |
| §9.14 never-dos | No delete/hide of required records by toggle, lapse, or lost password; no ratio counting of volunteers; no "on my way" sign-out; nothing auto-filed | DB constraints + RLS | — | `never_*` | every phase | ⬜ |
