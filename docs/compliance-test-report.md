# Compliance test report — O. Reg. 137/15 full sweep

*Audit date: 2026-08-30. Scope: every section of [tucked-ontario-requirements.md](../references/tucked-ontario-requirements.md) checked against the schema, the RPCs, the screens, and the automated proof. Test evidence: **542 pgTAP tests across 25 suites** (`supabase/tests/`), 79 domain tests (`packages/domain/test/`), all green on this date. Verdicts: ✅ built and machine-proven · 🔶 built, partially proven or partially built · ⬜ not built (phased per the build plan) — an honest ⬜ beats a decorative ✅.*

Run the proof yourself:

```bash
pnpm exec supabase test db
```

## The verdict table

| Rule | What the regulation demands | Where it lives | Machine proof | Verdict |
|---|---|---|---|---|
| Sched. 1 | Age-group ratios, max group, qualified staff | `age_group` + `domain/ageGroups` | `sched1_*` (domain, 10) | ✅ |
| ss. 8–10 ratios | Live counts; reduced-ratio windows and floors; volunteers/students/RCs never counted; 6+ children ⇒ 2 staff | `staff_shift` via `record_staff_shift`; `domain/ratios` | `s8_no_direct_shift_inserts`, `s8_volunteer_never_counted_in_ratio`, `s8_rece_counted_in_ratio`, `s8_volunteer_cannot_record_attendance` + 21 domain ratio tests | ✅ |
| s. 11 supervision | Adult supervision; face-to-name headcounts at moments that matter | `headcount_check` + `record_headcount` | `s11_transition_headcount_records_with_valid_pin`, `s11_missing_snapshot_names_the_child`, `s11_volunteer_cannot_record_headcounts`, `s11_parents_never_see_headcounts` | ✅ |
| s. 32 health obs | Observed on arrival before joining others; parent-reported symptoms recorded | sign-in flow prompt → `care_log(health_observation)` | `s32_health_observation_with_parent_report` | ✅ |
| s. 33.1 sleep | Back-to-sleep; direct visual checks < 24 mo in infant/toddler/family rooms only; rest ≤ 2 h | DB-gated `care_log(sleep_check)`; nap board | `s33_1_sleep_check_under_24m_in_toddler_room_ok`, `s33_1_sleep_check_rejected_at_24_months_plus`, `s33_1_sleep_check_rejected_in_preschool_room` | ✅ |
| s. 35 immunisation | Immunisation per the local MOH, or a physician/NP-signed medical exemption, or a NOTARISED conscience/religious exemption, or attends school (school-age only) | `immunisation_record` append-only ledger, `current_immunisation` latest-wins view, `record_immunisation` RPC; register on the children page, status in the child detail and the s. 72(6) MOH copy, console home exception for children with nothing on file | `s35_medical_exemption_names_the_practitioner`, `s35_conscience_exemption_must_be_notarised`, `s35_attends_school_never_covers_an_infant`, `s35_current_status_is_the_latest_row`, `s35_history_is_a_ledger`, `never_immunisation_rows_edited` + 8 more | ✅ |
| s. 36 sick child | Separated, parent contacted, taken home — all recorded; if urgent and no parent, seen by a physician or RN; exclusion and return follow the centre's illness policy; public health notified, orders kept and forwarded to the program advisor | `illness_policy` (auto-seeded per centre, leadership-editable — the return criteria on every exclusion are **copied from it**, never typed freehand), `health_exclusion` + append-only `health_exclusion_contact` (every attempt, not just the successful one), `public_health_notification` with a computed advisor deadline; **an excluded child cannot be signed in** — enforced inside `app.attendance_event_rules` beside the s. 50 restricted-person block, and cleared only by a PIN-signed judgment; room-device symptom picker, console Illness & exclusions, family card carrying the return criteria | `s36_an_excluded_child_cannot_be_signed_in`, `s36_the_refusal_says_what_has_to_be_true_first`, `s36_only_the_excluded_child_is_blocked`, `s36_going_home_is_never_blocked`, `s36_coming_back_early_needs_a_written_reason`, `s36_the_clearance_names_who_made_the_call`, `s36_where_the_child_waits_is_part_of_the_record`, `s36_an_unanswered_call_is_not_a_parent_reached`, `s36_an_order_reaches_the_program_advisor_within_two_business_days`, `s36_an_order_is_never_closed_before_the_advisor_has_it` + 28 more | ✅ |
| s. 36(4) accident | Report with **evidence the parent received a copy** | `accident_report` + ack RPC; DB trigger creates the Now alert | `s36_4_accident_report_recorded`, `s36_4_head_injury_requires_concussion_note`, `s36_4_parent_acknowledges_copy`, `s36_4_acknowledgement_names_the_parent_with_timestamp`, `s36_4_stranger_cannot_acknowledge`, `s36_4_acknowledged_report_never_changes` | ✅ |
| s. 37 DWR | Dated entry every operating day; incidents/drills/accidents cross-referenced; human close | `daily_written_record`, 06:00 pg_cron draft, and `app.dwr_compose` — the draft is now **arithmetic over the day's own rows** (attendance, outdoor minutes, menu substitutions, sleep checks, medication, headcounts) with every incident **quoted verbatim** from the cross-reference its trigger wrote at the time, so the summary and the reference cannot disagree. Deterministic, never generated (§9.14). Console shows a live preview and a Redraft button; a closed day refuses even a redraft | `s37_the_summary_quotes_the_cross_reference_word_for_word`, `never_the_daily_record_draft_is_deterministic_not_generated`, `s37_a_quiet_day_says_so_in_one_line`, `s37_and_a_closed_record_cannot_even_be_redrafted`, `s37_one_of_something_never_reads_as_1_childs`, `s37_accident_cross_referenced_into_daily_record`, `s37_close_names_the_human`, `s37_closed_record_never_changes` + 20 more | ✅ |
| s. 38 serious occurrence | CCLS within 24 h; anonymised 10-business-day posting; updates; human files; CYFSA CAS duty | `serious_occurrence` (+ children/updates/postings), 24 h clock from `aware_at`, `statutory_holiday` + `app.add_business_days`, hourly reminder cron; console page + home/mobile warnings | `s38_ccls_clock_is_24_hours_from_awareness`, `s38_ccls_number_is_the_evidence_of_filing`, `s38_no_close_before_filing`, `s38_posting_rejected_when_it_names_a_child`, `s38_ten_business_days_skip_weekends_and_labour_day`, `cyfsa_allegations_never_posted`, `cyfsa_no_close_without_cas_report`, `s38_deadline_warning_sent_exactly_once`, `s38_overdue_notice_sent_exactly_once` + 17 more | ✅ recording/clock/posting; CCLS filing itself stays human by design |
| ss. 39, 39.1, 52 plans | Anaphylaxis / medical / special-needs plans developed with the parent, agreed BEFORE implementation, annually reviewed; centre anaphylaxis policy; posted allergy list (s. 43(3)) | `individualised_plan` (versioned, content-immutable), `dietary_restriction`, live `allergy_list` view; console Plans page + printable posting, family in-app agreement, room Allergies sheet, offline evacuation cache | `s39_anaphylaxis_plan_requires_allergens`, `s39_anaphylaxis_plan_requires_emergency_procedure`, `s39_1_medical_plan_requires_emergency_procedure`, `s52_special_needs_plan_requires_supports`, `s52_new_plan_starts_as_draft_awaiting_agreement`, `s52_consenting_parent_agrees_in_app`, `s52_non_consenting_member_cannot_agree`, `s43_3_draft_plan_allergens_already_on_the_list`, `s43_3_list_unions_plans_and_restrictions`, `never_plan_content_edited` + 10 more | ✅ |
| s. 40 medication | Dose + schedule/symptoms required ("as needed" alone invalid); blanket items logged; revocation; expiry | `medication_authorisation`, `medication_administration` | `s40_as_needed_alone_is_insufficient`, `s40_dose_plus_schedule_authorised`, `s40_blanket_items_are_logged`, `s40_no_administration_after_revocation`, `s40_expired_medication_never_administered`, `s40_administrations_never_edited` + 5 more | ✅ |
| ss. 42–44 nutrition | Menus for the current **and** following week, posted where parents can see them; substitutions noted at the time; posted menus kept 30 days; infants fed per the parent's written instructions; special diets likewise | `menu_week` (frozen once posted — the day's change is a substitution, not an edit), `menu_item`, `menu_substitution` (snapshots the planned item beside what was served), `feeding_instruction` (must come from a consenting household member, one live per child and kind); console Menus page with printable posting + home exception, family "Today's menu" card, room-device substitution recording, feeding instructions in the room food sheet | `s42_posted_menu_is_frozen`, `s42_substitutions_never_recorded_ahead_of_the_day`, `s42_substitution_snapshots_what_was_planned`, `s42_families_see_the_posted_menu`, `s42_families_never_see_next_weeks_draft`, `s42_posted_menus_never_deleted`, `s44_instructions_come_from_a_consenting_parent`, `s44_one_live_instruction_per_child_and_kind` + 14 more | ✅ |
| s. 45 parent handbook | Every parent gets a handbook containing all 14 required items, including fees, CWELCC status, safe arrival and dismissal, the waiting-list policy, the anaphylaxis policy and the program statement | `handbook_section_spec` (the required list is **jurisdiction data**, not code), `handbook_content` (working draft), `handbook_version` + `handbook_version_section` (issued and immutable), `handbook_acknowledgement` (per parent, per version), `handbook_outstanding` view; two sections are read from the live record at publish so the handbook can never contradict it — the anaphylaxis policy (s. 39) and the CWELCC sentence (from `centre.cwelcc_enrolled`); console Handbook page (checklist, issue, printable, version history, who-has-it) + home exception; family app reads it and acknowledges in one tap | `s45_incomplete_handbook_is_never_issued`, `s45_anaphylaxis_section_is_the_live_policy`, `s45_cwelcc_section_cannot_contradict_the_record`, `s45_issued_handbook_is_never_edited`, `s45_identical_handbook_is_never_reissued`, `s45_a_household_viewer_cannot_acknowledge_for_the_family`, `s45_acknowledged_family_drops_off_the_outstanding_list`, `s45_required_sections_are_jurisdiction_data` + 21 more | ✅ |
| s. 47 outdoor play | Two hours outdoors a day, weather permitting, for a child in care six hours or more; a child kept indoors needs a physician's or parent's written instruction | `outdoor_requirement` (the minutes and the age floor are **jurisdiction data**), `outdoor_period` (measured: the room device records going out and coming in — no API anywhere accepts a number of minutes), `outdoor_shortfall` (the "weather permitting" reason, cross-referenced into the DWR), `outdoor_exemption` (physician names the physician; parent must be a consenting household member), `outdoor_day` view + `outdoor_by_child` (intersects each child's own attendance, and sizes the requirement to the day the child will actually have had rather than the hours elapsed so far); room-device Outside/Come-in toggle with a live counter, console Outdoor play page + home exception | `s47_no_one_can_type_the_number_of_minutes`, `s47_a_child_is_credited_only_with_the_time_they_were_actually_there`, `s47_a_child_still_here_is_owed_the_day_they_will_have_had`, `s47_a_finished_period_is_never_edited`, `s47_a_short_day_is_never_recorded_without_a_reason`, `s37_outdoor_shortfall_cross_referenced_into_the_daily_record`, `s47_staff_cannot_keep_a_child_in_on_an_aunts_say_so`, `s47_the_age_floor_is_part_of_the_rule_pack` + 24 more | ✅ (age floor flagged for regulatory review — see below) |
| ss. 46, 48–49 program | Program statement (HDLH), infants separated during active play | program statement is a handbook section (s. 45) | `s46_outdoor_skip_records_weather_reason` | 🔶 |
| s. 50 safe arrival/dismissal | Expected-but-absent chase; release only to authorised persons; restrictions hard-block | `pickup_restriction` SQL block inside `record_attendance` | `s50_restricted_pickup_blocked_at_sql`, `s50_release_to_authorised_parent_ok` | ✅ |
| ss. 53–64 staff files | RECE, first aid/CPR, VSC obtained before starting and renewed on the fifth anniversary, offence declaration every year without one, health assessment and immunisation; volunteers never alone / never in ratio | `staff_requirement` (**a rule pack keyed by jurisdiction AND role**, so the file reports what is *missing*, not just what is present), `credential` with the s. 60 arithmetic derived rather than typed, `staff_document` + the private `evidence` bucket **re-scoped to the subject and leadership only** (0012 had let any care-staff member read a colleague's police check), `staff_file_gaps()` and `offence_declaration_gaps()`; console staff file with upload and a printable per-person view | `s60_the_fifth_anniversary_is_derived_not_typed`, `s60_a_stale_check_is_refused`, `s61_an_offence_declaration_names_its_year`, `s53_a_colleague_never_reads_your_police_check`, `s53_the_bucket_policy_is_scoped_to_leadership_and_the_subject`, `s58_a_volunteer_is_never_in_ratio_so_never_needs_first_aid`, `s53_an_empty_file_reports_every_absence`, `s53_staff_documents_are_never_deleted` + 26 more | ✅ |
| s. 72(1) children's record | All 11 items; missing = never blank | `child_record_item` typed items | `s72_1_enrolment_incomplete_until_items_answered`, `s72_1_never_blank`, `s72_1_no_item_left_blank`, `s72_1_supervisor_verifies_every_item` | ✅ |
| s. 72(3) attendance | Actual times or "absent"; corrections carry who/when/why; continuous across rooms; offline | `attendance_event` append-only + corrections | `s72_arrive_records_with_valid_pin`, `s72_depart_requires_same_day_arrive`, `s72_actual_time_always_present`, `s72_no_updates_only_corrections`, `s72_correction_must_reference_same_child` | ✅ |
| s. 72(5) retention | 3 y post-discharge; financial 6 y (O. Reg. 138/15 s. 27.1); anonymise after — never before, never a delete | `retention_clock` (discharge trigger) + `app.anonymise_child` + nightly sweep; identity scrubbed (name, DOB day/month, photos, record-item content, free text in logs/reports/plans/stories/notifications, headcount name-snapshots) while every regulated row survives; immutability triggers admit only the anonymiser and close behind it; financial ledger append-only forever | `s72_5_sweep_anonymises_exactly_the_mature_clock`, `s72_5_acknowledged_accident_report_scrubbed_through_the_lock`, `never_attendance_rows_deleted_by_retention`, `never_early_anonymisation_even_called_directly`, `s72_5_escape_hatch_closes_behind_the_anonymiser` + 14 more; `never_*_deleted` in every suite; 4 domain retention tests | ✅ |
| CWELCC fees | Base fee capped for eligible children (under 6, or turning 6 before 30 June); non-base fees restricted; 30 days' written notice to every parent and employee to leave the system | `cwelcc_parameter` (**the cap is data, with effective dates**), `app.cwelcc_eligible` (computed from the date of birth, never ticked), `fee_schedule` and `fee_charge` both refuse a base fee over the cap, `fee_item` cannot express a compulsory non-base fee (a check constraint), `cwelcc_withdrawal_notice` (30-day constraint) with per-person delivery proof | `cwelcc_a_base_fee_above_the_cap_is_refused`, `cwelcc_a_charge_over_the_cap_is_refused_even_when_the_schedule_was_right`, `cwelcc_a_compulsory_non_base_fee_cannot_even_be_written_down`, `cwelcc_turning_six_before_the_june_cutoff_keeps_eligibility_to_the_cutoff`, `cwelcc_leaving_takes_thirty_days_written_notice`, `cwelcc_delivery_to_each_person_is_provable` + 10 more | ✅ |
| CRA receipts | Annual receipt naming the payer, the child, the amount, the period and the provider's business number | `cra_receipt` + `cra_receipt_line`, issued from payments **received** and split across children by what each was charged; immutable once issued (a correction is a replacement); names are **snapshotted** so the six-year financial record survives the three-year children's-record anonymiser, then redacted by `app.run_financial_sweep` at six years | `cra_the_receipt_is_what_was_received_not_what_was_billed`, `cra_payments_are_split_across_the_children_by_what_each_was_charged`, `cra_an_issued_receipt_is_never_altered`, `cra_a_receipt_needs_the_licensees_business_number`, `oreg138_the_receipt_snapshot_outlives_the_childs_record`, `oreg138_a_financial_record_is_never_redacted_early`, `oreg138_the_amount_stays_and_the_names_go` | ✅ **No SINs stored** — a licensed centre's receipt carries a business number; we will not hold a SIN beside children's photographs |
| s. 72(6) MOH export | Items 2, 3, 8, 9 exactly | child-record print view | manual print check | 🔶 |
| s. 73 consent | Enrolment never blocked on an optional consent | `consent` model | `s73_enrolment_completes_with_every_optional_consent_declined`, `s73_care_consent_still_required`, `s73_consent_reversible_later`, `s73_superseded_consent_rows_are_kept` | ✅ |
| s. 75.1 waitlist | No fee or deposit for a place; a policy setting out the admission order; a family learns its position without learning anyone else's | `waitlist_entry` + append-only `waitlist_event`; **no column on any waitlist table can hold money** and a catalogue test keeps it that way; position is computed by `app.waitlist_rank` (sibling → subsidy referral → date joined) and never stored; `joined_at` is immutable and a priority change demands a reason; `waitlist_self_check(code)` answers an anonymous family, `my_waitlist_positions()` answers a signed-in one, and the ranked view is care-staff-only because a window function would tell a family they are first; closed enquiries anonymised at 12 months by `app.run_waitlist_sweep` (02:45 cron) | `s75_1_no_waitlist_column_can_hold_a_fee`, `s75_1_position_follows_the_published_order`, `s75_1_the_moment_a_family_joined_never_moves`, `s75_1_only_leadership_moves_a_family_up_the_list`, `s75_1_moving_a_family_up_is_on_the_permanent_record`, `s75_1_an_enquiring_family_never_reads_the_list`, `s75_1_a_family_is_never_told_they_are_first_by_a_view_that_cannot_see_the_list`, `s75_1_a_wrong_code_reveals_nothing_at_all`, `s75_1_the_nightly_sweep_anonymises_what_is_due` + 29 more | ✅ |
| s. 82(2) electronic records | Staff and Ministry can always get in; offline on premises; never "ask your admin to unlock" | offline queue + zero-network evacuation cache; `break_glass_access`: any care-staff member opens a PIN-signed, reason-required, READ-ONLY 24-hour unlock of the inspection registers (audit trail, serious occurrences, staff files, retention clocks) — loud to leadership, audited, self-expiring, closable; `admin_grant_supervisor` restores supervisor access when the supervisor is gone for good | `s82_2_staff_files_locked_before_break_glass`, `s82_2_care_staff_opens_break_glass`, `s82_2_break_glass_grants_no_writes`, `s82_2_supervisor_alerted_on_every_open`, `s82_2_access_expires_by_itself`, `s82_2_families_cannot_break_glass`, `s82_2_every_break_glass_on_permanent_record`, `s82_2_platform_admin_restores_continuity` + 11 more | ✅ (on-device airplane-mode script still pending) |
| Parts 4 & 10 calendar | Fire drills (monthly), alarm/equipment tests, fire procedure posted, emergency policy review, playground inspections daily/monthly/annual (CSA Z614), first-aid kit, water temperature — with written records; repair log, hazards restricted until fixed | `compliance_task` auto-seeded on centre creation, `compliance_completion` append-only (note required — the completion IS the written record), `compliance_issue` with DWR cross-ref; drill headcounts complete the drill task automatically; console calendar + room-device checks card | `part4_10_every_centre_gets_the_standard_schedule`, `part10_completion_note_never_blank`, `part4_monthly_cadence_advances_one_month`, `part4_drill_headcount_completes_the_drill_task`, `part10_hazard_recorded_with_restriction`, `s37_hazard_cross_referenced_into_dwr`, `part4_10_only_leadership_changes_the_schedule`, `never_completion_records_deleted` + 9 more | ✅ |
| PIPEDA | Minimum collection; access scoped to need; Canadian hosting | RLS everywhere; **platform admins see tenancy and money, never children** | `rls_enabled_on_every_public_table`, `pipeda_platform_admin_sees_no_children`, `pipeda_platform_admin_sees_no_attendance`, `pipeda_platform_admin_sees_no_households`, `s72_parent_sees_own_children_only`, `s53_volunteer_sees_no_children` | ✅ for what's built |

## The never-do list (§9.14), proven

| Never | Proof |
|---|---|
| A required record deleted, hidden, or locked by a lapsed subscription or module toggle | `never_cancelled_billing_hides_records` and `never_cancelled_billing_blocks_writes` — the platform suite cancels a centre's subscription and then proves the supervisor still reads children and still records attendance. **No RLS policy anywhere reads `centre_subscription`.** Plus per-table `never_*_deleted` tests. |
| Delivery claimed without evidence | `s36_4_acknowledgement_names_the_parent_with_timestamp` |
| Volunteer/student/RC counted in ratio | `s8_volunteer_never_counted_in_ratio` (DB) + domain ratio tests |
| "On my way" sign-out | No API accepts it: `record_attendance` records only arrive/depart/absent at actual times (`s72_actual_time_always_present`) |
| Enrolment blocked on optional consent | `s73_enrolment_completes_with_every_optional_consent_declined` |
| Monitor/AI substitutes for direct checks or the human close | Sleep checks are per-child rows recorded by a PIN-signed human; `s37_close_names_the_human` |
| Auto-filing to CCLS/City/College/CRA | Nothing files anywhere; the DWR close and every regulated write names a human |

## Jurisdictions beyond Ontario

The `jurisdiction` table (0019) declares which regulator's rule pack a centre runs under. **CA-ON is the only implemented pack**; CA-MB and CA-QC are seeded as `planned`/inactive and `admin_create_centre` refuses to create a centre in a jurisdiction until its pack is `implemented` — proven by `planned_jurisdictions_reject_centres_until_implemented`. Adding a province or US state means: a requirements document like the Ontario one, a preset in `packages/domain/src/presets.ts`, jurisdiction-gated DB rules where they differ (sleep-check ages, ratios), and flipping the row to `implemented`/active. Nothing about tenancy, billing, or the app shell changes.

## Gaps that matter next (in order)

1. **Service-system-manager reporting (Toronto Children's Services)** — the fee ledger holds the numbers, but the City's actual file formats have to come from discovery. Deliberately not invented.
2. **On-device airplane-mode verification script** — the offline queue and evacuation cache are built and field-tested; the scripted quality pass on a real device is still owed.
3. **A parent-facing web fallback** — families are mobile-only by design. Worth revisiting with a pilot centre that has a parent without a smartphone.

**The full inventory of what is not built** — deliberate non-goals, genuine gaps, code that is written but not wired, partial areas, and every seeded value still needing a human to check it against the regulation — is [references/not-built.md](../references/not-built.md).

**One item needs a human with the regulation open:** the s. 47 rule pack sets an 18-month age floor and a 6-hour care threshold, following the summary in [tucked-ontario-requirements.md](../references/tucked-ontario-requirements.md) rather than the section text itself. Both are rows in `outdoor_requirement`, so confirming or correcting them is a one-line change with no code involved — but they should be confirmed before a real inspection.

Every Phase 0/1 compliance item is built and machine-proven; from the Phase 2 list, the Parts 4/10 compliance calendar, the ss. 42–44 menus module, the s. 45 parent handbook, the s. 75.1 waiting list and the s. 47 outdoor play log have now landed too.

*(Landed 2026-08-30, in order: s. 38 serious occurrences (26 tests), ss. 39/39.1/52 + s. 43(3) plans and allergy list (20 tests), the s. 72(5) retention anonymiser (19 tests), the s. 45 parent handbook (29 tests), the s. 75.1 waiting list (38 tests), the s. 47 outdoor play log (32 tests), and the s. 36 illness, exclusion and public-health module (38 tests), the CWELCC fee and CRA receipt module (43 tests), the ss. 53–64 staff files with document uploads (34 tests), push delivery (22 tests), the s. 37 daily-record composer (21 tests), and Expo delivery receipts (20 tests). Known limitation, documented deliberately: serious-occurrence descriptions are not scrubbed by the child anonymiser — they are Ministry-filed records with their own lifecycle; free text there should avoid naming children, which the console page's guidance encourages.)*
